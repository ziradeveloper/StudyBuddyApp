import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ziradeveloper.studybuddy', // ✅ unique ID
  appName: 'study-buddy',
  webDir: 'dist'
};

export default config;
